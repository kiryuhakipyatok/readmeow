input {  
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
    remove_field => ["message"]
  }
}

output {
  if [type] == "http" {
    elasticsearch {
      hosts => ["http://elasticsearch:9200"]
      data_stream => true
    }
  } else if [type] == "app" {
    elasticsearch {
      hosts => ["http://elasticsearch:9200"]
      data_stream => true
    }
  } else {
    elasticsearch {
      hosts => ["http://elasticsearch:9200"]
      data_stream => true
    }
  }
}
